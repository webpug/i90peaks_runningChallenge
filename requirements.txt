# Pinned constraints that matter (richdem needs numpy<2 and pkg_resources<80).
# Recreate the env with:  uv venv --python 3.10 .venv && uv pip install -r requirements.txt
numpy<2
richdem==0.3.4
setuptools<80

rasterio
rioxarray
xarray
scipy
geopandas
shapely
pyproj
osmnx>=2
requests
matplotlib
folium
tqdm
pandas
mercantile
pillow
networkx
